# Phase Tracker

Living snapshot of **where Folio Interactive is in the build**, derived from **Current Phase** and related guidance in `CLAUDE.md`. Update this file whenever phase boundaries move; agents should read it alongside `CLAUDE.md` before recommending work.

---

## Current phase: **Infrastructure**

Per `CLAUDE.md`, the initiative is in an **infrastructure build**: the Obsidian vault and operating context exist so product, curriculum, and build work can run in one disciplined place.

---

## Complete

- **Vault skeleton** — Top-level folders (`00 - Inbox` through `07 - Archive`) and the `05 - Build` subtree (including `review-queue/`) exist for intake, specs, and artifacts.
- **Persistent operator brief** — `CLAUDE.md` at the vault root defines product, locked decisions, pipeline, pedagogy order, brand voice, vault map, and working rules.
- **Core layer bootstrapped** — `01 - Core/` contains this tracker, the identity note, and the decisions log so phase and commitments are not only in the root brief.

---

## In progress

- **Populating `01 - Core/`** — Identity, decisions log, and phase tracker are drafted from `CLAUDE.md`; further core references (e.g. deeper brand, technical constraints) may still be added as they stabilize.
- **Preparing for systems migration** — Selecting which orchestrators, skills, and protocols move into `03 - Systems/` and in what order, without yet treating that tree as the single source of truth.

---

## Next (in sequence)

1. **Finish `01 - Core/`** — Any remaining permanent reference notes Rob wants at this layer (e.g. pilot definition, educator roster, success metrics) so recommendations always have a local anchor.
2. **Migrate verified systems into `03 - Systems/`** — Copy or re-home **orchestrators**, **skills**, and **protocols** only after they are **verified** for this vault’s layout; deprecate superseded versions to `07 - Archive/` rather than deleting.
3. **Begin `CLAUDE.md`-driven task execution** — Day-to-day work follows: read `CLAUDE.md` → check `01 - Core/` → respect **Locked Decisions** → route new drafts through **`05 - Build/review-queue/`** before wider placement.
4. **Curriculum and build surface** — Stand up first **World Geography / Map Skills** materials and **web** artifacts under `04 - Curriculum/` and `05 - Build/` per pipeline stages (Draft → Ready for Review → Approved).

---

## Out of scope until post-pilot (reminder)

**Native mobile apps** and any roadmap branch that contradicts **web-only** or **K–12-only** scope stay closed until the pilot phase explicitly reopens them — see `decisions-log.md` and `CLAUDE.md`.
